# Icone na barra de tarefas

O Rony registra um AppUserModelID proprio no Windows e aplica `app.ico` na janela nativa para evitar que a barra de tarefas mostre o icone do Python.

O instalador tambem define o mesmo AppUserModelID nos atalhos criados pelo Inno Setup.
